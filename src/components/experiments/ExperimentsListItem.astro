---
import FormattedDate from "../shared/FormattedDate.astro";
export interface Props {
    date: Date;
    image: string;
    title: string;
    slug: string;
    featured?: boolean;
}

const { date, image, title, slug, featured = false } = Astro.props;
---

<li class:list={["relative list-none", featured ? "mb-8" : ""]}>
    <a href={`/playground/${slug}`}>
        <div
            class:list={[
                "group aspect-h-7 aspect-w-10 block w-full overflow-hidden rounded-lg bg-gray-100",
                "focus-within:ring-2 focus-within:ring-indigo-500 focus-within:ring-offset-2 focus-within:ring-offset-gray-100",
                featured ? "aspect-h-4" : "",
            ]}
        >
            <img
                src={image}
                alt=""
                class="pointer-events-none object-cover"
                transition:name={`hero-image-${slug}`}
            />
        </div>

        <div class={featured ? "text-center text-md" : "text-sm"}>
            <p
                class="pointer-events-none mt-2 block truncate font-medium text-gray-900"
                transition:name={`experiment-title-${slug}`}
            >
                {title}
            </p>
            <p
                class="pointer-events-none block font-medium text-gray-500"
                transition:name={`experiment-date-${slug}`}
            >
                <FormattedDate date={date} />
            </p>
        </div>
    </a>
</li>
